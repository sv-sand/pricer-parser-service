spring:
  application.name: parser-service
  output.ansi.enabled: ALWAYS

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.hibernate: ERROR
  file:
    path: ./logs
    max-history: 5
    max-size: 10MB
    total-size-cap: 100MB
